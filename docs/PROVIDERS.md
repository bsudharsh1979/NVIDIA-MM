# Providers

| Slot | Options |
| --- | --- |
| Tutor | demo, openai, nim, huggingface |
| Voice | off, elevenlabs, sarvam, openai_realtime |
| Research | off, perplexity |

Health matrix is `/api/providers`. Fallback is **disclosed**. Cost traces: `/api/cost`.
