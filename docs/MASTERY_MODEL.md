# Mastery model

Viewing is low weight. Predict / diagnose / apply / design / teach-back dominate.

```
score = 1 - exp(-weighted_evidence)
```

FSRS-inspired `stability` / `difficulty` set `next_review`. Incorrect attempts create `ReviewItem` rows shown on Home as **Reviews due**.
