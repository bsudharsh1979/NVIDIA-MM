# Testing

- `pytest` — twins, evidence, ingest, API smoke
- `vitest` — evidence labels
- Playwright spec in `tests/e2e/smoke.spec.ts` (start API+web first)

Evidence integrity tests prove simulations are not labeled actual, imports preserve raw text, confounders fire, notebooks are not executed.
