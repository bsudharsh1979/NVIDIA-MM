# Omniverse Kit extension stub

Subscribe to `OMNIVERSE_BRIDGE_URL` (default `ws://localhost:8010/twin`) and apply `prims` from TwinStateEngine.

Do not invent disaggregated-inference visuals as NVIDIA course facts — this course is multimodal fusion / VSS / CILP.
